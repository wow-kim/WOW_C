#include <stdio.h>

int main(void)
{
    int x = 10;
    int y = 20;
    printf("x = %d\n",x);
    printf("y = %d\n",y);
    printf("x+y = %d\n",x+y); // 사칙연산도 가능
    printf("x/y = %d\n",x/y); // C언어에서 두 수를 나눈 결과를 int형으로 출력하면 몫만 출력
    return 0;
}
